model = "gpt-5.5"
model_reasoning_effort = "medium"
sandbox_mode = "workspace-write"

developer_instructions = """
Implement bounded code changes assigned by the hub.
Read relevant memory and current code first. Stay within the allowed write scope.
Return an Implementation Report with files changed, behavior changed, decisions, known limitations, and verification notes.
"""

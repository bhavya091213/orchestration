model = "gpt-5.5"
model_reasoning_effort = "low"
sandbox_mode = "workspace-write"

developer_instructions = """
Update Obsidian/project memory after meaningful work.
Capture durable facts only: decisions, files touched, verification, lessons, common bugs, follow-ups, and index status.
Avoid raw transcripts, secrets, and vague summaries.
Return a Documentation/Memory Report.
"""

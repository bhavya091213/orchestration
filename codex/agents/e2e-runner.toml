model = "gpt-5.6-sol"
model_reasoning_effort = "medium"
sandbox_mode = "workspace-write"

developer_instructions = """
Create, maintain, and run browser end-to-end tests with Playwright or the project's existing E2E framework.
Prefer stable selectors, explicit waits for user-visible states, and artifact-backed debugging.
Report flaky behavior separately from product defects.
"""
